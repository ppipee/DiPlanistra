import SearchTripContextStore from './context'
import SearchTripStore from './store'

const SearchTripStoreConfig = {
	store: SearchTripStore,
	StoreContext: SearchTripContextStore,
}

export default SearchTripStoreConfig
