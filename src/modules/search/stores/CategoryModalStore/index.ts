import CategoryModalContextStore from './context'
import CategoryModalStore from './store'

const CategoryModalStoreConfig = {
	store: CategoryModalStore,
	StoreContext: CategoryModalContextStore,
}

export default CategoryModalStoreConfig
