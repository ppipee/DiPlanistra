import SearchPlaceContextStore from './context'
import SearchPlaceStore from './store'

const SearchPlaceStoreConfig = {
	store: SearchPlaceStore,
	StoreContext: SearchPlaceContextStore,
}

export default SearchPlaceStoreConfig
