import PlannerStoreContext from './context'
import PlannerStore from './store'

const PlannerStoreConfig = {
	store: PlannerStore,
	StoreContext: PlannerStoreContext,
}

export default PlannerStoreConfig
