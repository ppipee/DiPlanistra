import FavoritePlaceStoreContext from './context'
import FavoritePlaceStore from './store'

const FavoritePlaceStoreConfig = {
	store: FavoritePlaceStore,
	StoreContext: FavoritePlaceStoreContext,
}

export default FavoritePlaceStoreConfig
