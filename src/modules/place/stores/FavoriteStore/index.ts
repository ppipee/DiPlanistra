import FavoriteStoreContext from './context'
import FavoriteStore from './store'

const FavoriteStoreConfig = {
	store: FavoriteStore,
	StoreContext: FavoriteStoreContext,
}

export default FavoriteStoreConfig
