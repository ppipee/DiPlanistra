import FavoriteEventStoreContext from './context'
import FavoriteEventStore from './store'

const FavoriteEventStoreConfig = {
	store: FavoriteEventStore,
	StoreContext: FavoriteEventStoreContext,
}

export default FavoriteEventStoreConfig
