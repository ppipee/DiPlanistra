import styled from 'styled-components'

import Gap from 'common/components/Gap'

const MAX_WIDTH = '850px'

export const PlannersContainer = styled(Gap)`
	max-width: ${MAX_WIDTH};
	width: 100%;
	margin: 0 auto;
`
