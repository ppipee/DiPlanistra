import React from 'react'

import BottomBar from 'common/components/BottomBar'

import TripOverviewButton from '../TripOverviewButton'

const PlannerBottomBar = () => {
	return (
		<BottomBar>
			<TripOverviewButton />
		</BottomBar>
	)
}

export default PlannerBottomBar
