import PlannersStoreContext from './context'
import PlannersStore from './store'

const PlannersStoreConfig = {
	store: PlannersStore,
	StoreContext: PlannersStoreContext,
}

export default PlannersStoreConfig
